========
Typedefs
========

@def x(u32).
@def edge(string, string).
@def reachable(string   ,  string).

@def with_input(u32) input.
@def with_output(u32) output.
@def with_many_qualifiers(u32) input output output input output.

---

(source_file
  (typedef (identifier)
    (type_list (type)))
  (typedef (identifier)
    (type_list (type) (type)))
  (typedef (identifier)
    (type_list (type) (type)))
  (typedef (identifier)
    (type_list (type)) (qualifier))
  (typedef (identifier)
    (type_list (type)) (qualifier))
  (typedef (identifier)
    (type_list (type))
      (qualifier) (qualifier) (qualifier) (qualifier) (qualifier)))
